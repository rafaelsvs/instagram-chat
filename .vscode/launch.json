{
  "version": "0.2.0",
  "configurations": [
    {
    "name": "🐳 Debug via Docker (attach)",
    "type": "python",
    "request": "attach",
    "connect": {
        "host": "localhost",
        "port": 5678
    },
    "pathMappings": [
        {
        "localRoot": "${workspaceFolder}",
        "remoteRoot": "/app"
        }
    ]
    }
  ]
}
